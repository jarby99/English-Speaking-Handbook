{
  "name": "english-speaking-handbook-thai-audio-app",
  "version": "1.0.0",
  "private": true,
  "description": "Static Thai audio practice app packaged for Android.",
  "scripts": {
    "android:sync": "cap sync android",
    "android:build:debug": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
